[pytest]
# Two trees, one suite. `tests/` is the calculation engine (the 37-step
# workflow); `apps/api/tests/` is the website's backend -- ingestion (Phase 3)
# and source review (Phase 4).
testpaths = tests apps/api/tests
